# Benchmarks

Only self-authored, permissively licensed, or permission-cleared benchmark assets may be committed. External benchmarks should be integrated through adapters that download data only after the user accepts the upstream terms; the repository must not silently mirror restricted assets.
