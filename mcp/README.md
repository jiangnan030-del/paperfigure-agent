# MCP adapter (planned)

The MCP layer will expose validated `render` and `audit` operations. It must not expose arbitrary shell or Python execution. The CLI/SDK contract will stabilize before this adapter is implemented.
